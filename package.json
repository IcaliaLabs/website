{
  "name": "icalialabs-website",
  "private": true,
  "version": "1.0.0",
  "description": "Icalia Labs marketing site — nearshore staff augmentation",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port=8080",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
